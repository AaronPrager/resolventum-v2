export const COOKIE_NAME = "rv_session";
export const SESSION_DAYS = 30;
/** Remembers whether the desktop menu is folded. Plain module: the layout (server) and the sidebar (client) both read it. */
export const SIDEBAR_COOKIE = "sidebar";
